# backend/requirements.txt
# ---------------------------------------------------------------
# All Python packages needed for the MailMind backend.
# Install with: pip install -r requirements.txt
# ---------------------------------------------------------------

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# --- Environment variables ---
python-dotenv==1.0.1

# --- AI: Google Gemini (native SDK — no OpenAI wrapper needed) ---
# google-genai is Google's official first-party Python SDK for Gemini.
# Cleaner API, stable, no compatibility layer issues.
google-genai>=2.0.0

# --- Google Gmail API + OAuth ---
google-api-python-client==2.134.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# --- Data validation ---
pydantic>=2.7.4

# --- HTTP requests (used by google libs) ---
requests==2.32.3

# --- Templating and Forms ---
jinja2>=3.1.4
python-multipart>=0.0.9
